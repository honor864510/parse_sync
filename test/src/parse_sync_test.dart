// ignore_for_file: prefer_const_constructors
import 'package:flutter_test/flutter_test.dart';

import 'package:parse_sync/parse_sync.dart';

void main() {
  group('ParseSync', () {
    test('can be instantiated', () {
      expect(ParseSync(), isNotNull);
    });
  });
}
